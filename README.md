# Weather Assistant – Starter Project

This repository provides the **complete starter scaffold** for building an AI-powered Weather Assistant using **Next.js (App Router)**, **TypeScript**, **Tailwind CSS**, and **Vercel AI SDK**.

It includes everything you need to begin immediately:

---

## ✅ Included in This Project

### **📁 Project Structure**

* **Next.js App Router** setup
* **Tailwind CSS** configuration
* `components/` folder containing:

  * `Conversation`
  * `Message`
  * `PromptInput`
  * `WeatherCard`
* `app/api/chat/route.ts` — fully structured API route ready for LLM + Weather API integration
* Example environment setup
* Clean folder structure, easy to extend

---

## 🚀 Running the Project Locally

### **1. Install dependencies**

```bash
npm install
```

### **2. Configure environment variables**

Create a file named `.env.local` in the project root.
Use .env.local.example as a reference for which keys are required.


### **3. Start the development server**

```bash
npm run dev
```

Open:

```
http://localhost:3000
```

---
